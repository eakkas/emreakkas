---
const locations = [
  { city: 'Vancouver', country: 'BC, Canada', flag: '🇨🇦' },
  { city: 'Bodrum', country: 'Turkey', flag: '🇹🇷' },
  { city: 'Gdańsk', country: 'Poland', flag: '🇵🇱' },
  { city: 'Cabarete', country: 'Dominican Republic', flag: '🇩🇴' },
];
---

<section class="py-24 bg-gray-50">
  <div class="max-w-5xl mx-auto px-6 text-center">
    <p class="text-sm font-medium text-blue-600 tracking-widest uppercase mb-4">Location</p>
    <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
      The globe is my playground.
    </h2>
    <p class="text-gray-500 mb-14 max-w-lg mx-auto leading-relaxed">
      You'll mostly find me splitting my time between these four places — each one offering
      something different, all of them home.
    </p>

    <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4">
      {locations.map((loc) => (
        <div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100 hover:shadow-md transition-shadow">
          <div class="text-4xl mb-3">{loc.flag}</div>
          <h3 class="font-semibold text-gray-900">{loc.city}</h3>
          <p class="text-sm text-gray-500 mt-1">{loc.country}</p>
        </div>
      ))}
    </div>
  </div>
</section>
